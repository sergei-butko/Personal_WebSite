node_modules/
.next/
out/
build/

*.log
npm-debug.log*

.DS_Store
*.pem
.vscode/
.idea/

.env*
!.env.example

next-env.d.ts
tsconfig.tsbuildinfo

# Ad-hoc Telegram page dumps; the tracked fixture lives in scripts/__fixtures__/
tg-sample.html

# Image bytes are never committed. They live in Cloudinary, uploaded by the
# sync scripts; only src/content/*.generated.ts is in git. This directory was
# 827 MB of duplicated photos before the migration — the ignore keeps a stray
# local run from putting it back.
/public/images/

# Personal working notes — must never reach a public repo.
CLAUDE.local.md
