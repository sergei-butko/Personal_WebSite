node_modules
.next
out
package-lock.json

# Hand-authored prose. Prettier's MDX parser still targets MDX v1, so it reads
# an MDX v3 comment `{/* ... */}` as markdown emphasis and rewrites it to
# `{/_ ... _/}`, which no longer compiles. It also reflows paragraphs, which is
# the author's call to make, not the formatter's.
src/content/posts

# Generated by the sync scripts — formatting these is pure churn, and the
# scripts do not emit Prettier style, so a formatted copy fails format:check
# again the moment the next sync runs.
src/content/photos.generated.ts
src/content/threads.generated.ts
src/content/photo-hashes.generated.ts

# Saved verbatim from t.me, and that is the point of them. Prettier rewrites
# HTML whitespace, and telegram-parse.ts reads text nodes — the caption parser
# turns `<br/>` into a newline and trims around it. Reformatting a fixture could
# change what the parser sees and quietly invalidate the test it exists to feed.
scripts/__fixtures__

# Personal notes, untracked.
CLAUDE.local.md
